requests>=2.31
python-dotenv>=1.0
playwright==1.56.0

# yfinance는 기본 파이프라인(collect_data.py)에서는 더 이상 쓰지 않는다
# (클라우드 샌드박스에서 curl_cffi/프록시 충돌 + 레이트리밋으로 불안정했음).
# scripts/yfinance_client.py를 로컬 등 대안으로 직접 쓰려면 별도 설치 필요:
# python3 -m pip install yfinance>=0.2.40
